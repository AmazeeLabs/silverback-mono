services:
  silverback_gatsby.feed_manager:
    class: Drupal\silverback_gatsby\FeedManager
    tags:
    - { name: service_collector, tag: silverback_gatsby_feed, call: addFeed }

parameters:
  silverback_gatsby.entity_feeds:
# Provide entity feed definitions in your custom module like this:
#    - type: node,
#      bundle: page
#      typeName: Page

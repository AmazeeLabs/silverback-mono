{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "pipeline": {
    "prep": {
      "dependsOn": ["^prep"],
      "inputs": ["src/**", "tsup.config.ts", "tsconfig.json"],
      "outputs": ["build/**"]
    }
  }
}

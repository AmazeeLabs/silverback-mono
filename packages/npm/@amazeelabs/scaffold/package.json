{
  "name": "@amazeelabs/scaffold",
  "version": "1.1.6",
  "description": "Scaffold dotfiles and packages for JS projects.",
  "main": "index.js",
  "author": "Philipp Melab",
  "license": "MIT",
  "private": false,
  "bin": {
    "amazee-scaffold": "./cli.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "esm": "^3.2.25"
  },
  "scripts": {
    "precommit": "lint-staged",
    "prepare": "tsc --project ./tsconfig.local.json --outDir dist && node cli.js",
    "test": "tsc --noEmit && jest --passWithNoTests && if [ -f test.sh ]; then ./test.sh; fi",
    "watch": "jest --watch"
  },
  "devDependencies": {
    "@amazeelabs/jest-preset": "^1.3.0",
    "@types/chalk": "^2.2.0",
    "@types/jest": "^26.0.20",
    "@types/mock-fs": "^4.13.0",
    "@types/node": "^14.14.22",
    "husky": "^5.1.3",
    "jest": "^26.6.3",
    "lint-staged": "^10.5.4",
    "mock-fs": "^4.13.0",
    "typescript": "^4.2.3"
  }
}

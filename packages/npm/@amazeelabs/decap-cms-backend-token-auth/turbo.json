{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "pipeline": {
    "prep": {
      "dependsOn": ["^prep"],
      "inputs": ["src/**", "tsconfig.json", "tsconfig.build.json"],
      "outputs": ["build/**"]
    }
  }
}

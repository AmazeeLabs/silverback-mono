{
  "name": "@amazeelabs/react-di",
  "version": "1.0.9",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@amazeelabs/jest-preset": "^1.3.0",
    "@amazeelabs/scaffold": "^1.1.6",
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "lint-staged": "^10.5.4",
    "typescript": "^4.0.5"
  },
  "scripts": {
    "prepare": "yarn amazee-scaffold && tsc --declaration --outDir ./dist",
    "precommit": "lint-staged",
    "test": "jest --passWithNoTests && if [ -f test.sh ]; then ./test.sh; fi",
    "watch": "jest --watch"
  }
}
